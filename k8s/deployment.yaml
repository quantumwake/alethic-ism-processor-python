apiVersion: apps/v1
kind: Deployment
metadata:
  name: alethic-ism-processor-python-deployment
  namespace: alethic
spec:
  replicas: 1 
  selector:
    matchLabels:
      app: alethic-ism-processor-python
  template:
    metadata:
      labels:
        app: alethic-ism-processor-python
    spec:
      volumes:
        - name: alethic-ism-processor-python-secret-config-volume
          secret:
            secretName: alethic-ism-processor-python-secret
            items:
              - key: .routing.yaml
                path: .routing.yaml
      containers:
      - name: alethic-ism-processor-python
        image: <IMAGE>
        imagePullPolicy: Always
        volumeMounts:
          - name: alethic-ism-processor-python-secret-config-volume
            mountPath: /app/repo/.routing.yaml
            subPath: .routing.yaml
            readOnly: true
        env:
          # Consumer Configuration for Pulsar
          - name: MSG_URL
            valueFrom:
              secretKeyRef:
                name: alethic-ism-processor-python-secret
                key: MSG_URL

          - name: MSG_TOPIC
            valueFrom:
              secretKeyRef:
                name: alethic-ism-processor-python-secret
                key: MSG_TOPIC

          - name: MSG_MANAGE_TOPIC
            valueFrom:
              secretKeyRef:
                name: alethic-ism-processor-python-secret
                key: MSG_MANAGE_TOPIC

          - name: MSG_TOPIC_SUBSCRIPTION
            valueFrom:
              secretKeyRef:
                name: alethic-ism-processor-python-secret
                key: MSG_TOPIC_SUBSCRIPTION

          # Database connection details
          - name: DATABASE_URL
            valueFrom:
              secretKeyRef:
                name: alethic-ism-processor-python-secret
                key: DATABASE_URL

      imagePullSecrets:
      - name: regcred
